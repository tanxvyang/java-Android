一。斐波那契序列

1 1 2 3 5 8 13 。。。
定义一个方法求第n项的值  test1

//递归调用
        自己调用自己
    public static int g(int n) {
           int ni;
        if (n == 1 || n == 2) {
            ni = 1;
        } else {
            ni=g(n-1) + g(n-2);
        }
        return ni;
    }




写一个方法求n的阶乘  test2

9！=1*2*3*4*...*9


二。引用类型   test3
        数组
                一组相同类型元素的集合，个数固定
        数组的声明
            int[]变量名
            java支持  int arr[]
1.分配空间以后会自动初始化，其值为0  0.0   false   \u0000  null
堆  存放 new出来的东西
栈  存放变量  局部变量


import java.util.Arrays;
  System.out.println(Arrays.toString(a));//arrays 显示数组所有元素



  Exception in thread "main" java.lang.ArrayIndexOutOfBoundsException: 8
错误原因：数组下标越界异常，出现错误需要检查数组下标


很多课程  test5
为每一门课程打分


三。动态数组  test5
  System.arraycopy(c,0,temp,0,c.length);//copy函数  （原数组，原数组起始位置，目标数组，目标数组起始位置，拷贝长度）